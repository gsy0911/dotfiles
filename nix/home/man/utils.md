# global aliases

- `C`: `| wc -l`
- `G`: `| grep --color=auto`

# utils

- `diffu`: `colordiff -u`
- `diffy`: `colordiff -y`
- `timer`: `date && time cat && date`

# makefile

- `mp`: `make | peco`
- `fis`: `make`

# node.js

- `nsc`: `cat package.json | jq ".scripts"`

# other

- `tenki`: `curl wttr.in/Tokyo`
